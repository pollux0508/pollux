<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.polluxframework.mapper.UserMapper">
	<select id="getUserByUnique" resultType="com.polluxframework.entity.User" parameterType="com.polluxframework.entity.User">
		SELECT   ID as 'id',
				 ORG_ID as 'orgId',
				 USER_CODE as 'userCode',
				 CN_NAME as 'cnName',
				 EN_NAME as 'enName',
				 LOGIN_NAME as 'loginName',
				 PASSWORD as 'password',
				 TYPE as 'type',
				 CREATOR as 'creator',
				 CREATE_TIME as 'createTime',
				 LAST_UPDATOR as 'lastUpdator',
				 LAST_UPDATE_TIME as 'lastUpdateTime'
		FROM     HZ_AP_USER
		WHERE	 1=1
		<if test="id!=null">
			AND  ID=#{id}
		</if>
		<if test="userCode!=null">
			AND	 USER_CODE=#{userCode}
		</if>
		<if test="loginName!=null">
			AND	 LOGIN_NAME=#{loginName}
		</if>
	</select>

	<select id="getUserList" resultType="com.polluxframework.entity.User">
		SELECT   ID as 'id',
				 ORG_ID as 'orgId',
				 USER_CODE as 'userCode',
				 CN_NAME as 'cnName',
				 EN_NAME as 'enName',
				 LOGIN_NAME as 'loginName',
				 PASSWORD as 'password',
				 TYPE as 'type',
				 CREATOR as 'creator',
				 CREATE_TIME as 'createTime',
				 LAST_UPDATOR as 'lastUpdator',
				 LAST_UPDATE_TIME as 'lastUpdateTime'
		FROM     HZ_AP_USER
	</select>
</mapper>